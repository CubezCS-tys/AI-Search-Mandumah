# pytest configuration
[pytest]
asyncio_mode = auto
markers =
    unit: No network, no Qdrant, no OpenAI. Mock the Searcher.
    integration: Requires live Qdrant (QDRANT_URL) and a valid OPENAI_API_KEY.
